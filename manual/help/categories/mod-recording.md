# Mod Recording

| Input | Action |
|---|---|
| `Space` | Toggle recording (stop recording) *(when recording mode active)* |
| `ctrl+P` | Toggle recording (alternate) *(when recording mode active)* |
| `A-Z/0-9` | Play notes via keyboard layout during recording *(when recording mode active, no Ctrl/Meta)* |