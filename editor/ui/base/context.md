# Context

- `button.ts`, Parent factory for all button element variants
- `container.ts`, Parent factory for all container element variants
- `index.ts`, Barrel re-export of base component factories
- `input.ts`, Parent factory for all input element variants
- `label.ts`, Parent factory for all label element variants
