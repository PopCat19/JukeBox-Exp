# Context

- `checkbox-input.ts` — Styled checkbox for use in forms
- `color-picker.ts` — Popover color picker with hex / HSL / OKLCH tabs and alpha slider
- `index.ts` — Barrel re-export of input variants
- `input-box.ts` — Input element wrapper with undo-aware change tracking
- `search-input.ts` — Styled text input for search/filter functionality
- `stepper-input.ts` — Number input with standard stepper styling
